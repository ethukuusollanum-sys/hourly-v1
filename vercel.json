{
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://hourly-tracker.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
